import { ADD_CARD, ADD_DECK } from '../utils/constants';

function entries (state = {}, action) {
  const { type } = action;

  switch (type) {

    default :
      return state
  }
}

export default entries
